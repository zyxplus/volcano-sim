# Reclaim 队列身份设计

## 目标

Reclaim 在队列排序变化后，仍按队列名定位源队列，确保扣减和回滚不会写入错误的队列。

## 设计

- reclaim 记录 victim 的 `QueueName`，不把切片下标作为持久身份。
- 需要修改队列状态时，按名称查找当前索引。
- `RunWithQueues` 可以继续原地排序队列切片。

## 验收

- 队列权重导致排序变化时，成功 reclaim 扣减正确源队列。
- reclaim 失败时，正确源队列恢复，其他队列不被修改。
- 全量测试通过。
