# Reclaim Dry-Run Plan

1. 扩展 API：Task 记录运行节点与所属 Queue；AllocationPlan 增加 Evictions。
2. 写失败测试：overused、reclaimable Queue 的 1-GPU Task 被 Evict，保障 Queue 的 1-GPU Gang 得到 Allocate。
3. 用同一 journal 暂时释放 victim 的 Node/Queue 资源并尝试 Allocate；不足 minAvailable 时同时 undo。
4. 全量测试、提交并合并。
